export class Contact {
  name: string;
  email: string;
  comment: string;

  constructor(name: string, email: string, comment: string) {
    this.name = name;
    this.email = email;
    this.comment = comment;
  }
}